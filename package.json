{
  "name": "pondoknusa",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "engines": {
    "node": ">=26"
  },
  "scripts": {
    "build": "tsc -b && npm run build --workspace=@pondoknusa/cli",
    "check:node": "node scripts/check-node.mjs",
    "pretest": "npm run check:node",
    "test": "vitest run",
    "test:watch": "vitest",
    "pretypecheck": "npm run check:node",
    "typecheck": "tsc -b",
    "docs:generate": "node scripts/generate-docs.mjs",
    "predocs:build": "npm run docs:generate",
    "predocs:dev": "npm run docs:generate",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "pondoknusa": "node packages/cli/dist/bin/pondoknusa.js",
    "prebenchmark": "npm run build",
    "benchmark": "node scripts/benchmark.mjs",
    "release:prepare": "node scripts/prepare-release.mjs"
  },
  "devDependencies": {
    "@hono/node-server": "^2.0.6",
    "@types/node": "^26.0.1",
    "express": "^5.2.1",
    "fastify": "^5.9.0",
    "hono": "^4.12.27",
    "typescript": "^5.8.3",
    "vitepress": "^1.6.3",
    "vitest": "^3.2.4"
  }
}
